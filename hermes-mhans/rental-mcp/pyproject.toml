[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rental-mcp"
version = "0.1.0"
description = "MCP server para Mario Hans Rental Fotográfico — arriendo de equipos fotográficos, Chile."
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0",
    "psycopg[binary]>=3.1",
    "psycopg-pool>=3.1",
    "pydantic>=2",
    "httpx>=0.27",
]

[project.scripts]
rental-mcp = "rental_mcp.__main__:main"

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "anyio[trio]",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["rental_mcp"]
